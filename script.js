let number = 1;

let start = performance.now(); // Armazena o instante de inicialização do loop para a solução do problema

// Algorítmo de solução
for (let i = 0; i = number; i++) {

};

let end = performance.now(); // Armazena o instante final em que o loop terminou de ser executado

// Cálculo do tempo de solução
let timeElapsed = start - end